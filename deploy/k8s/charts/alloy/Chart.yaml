apiVersion: v2
name: alloy
description: >-
  Grafana Alloy as a log-shipping DaemonSet (RFC-0003 DK6, ADR-0017): one
  collector per node, tailing the pods scheduled on that node and writing to
  Loki.
type: application
version: 0.1.0
appVersion: "1.17.1"
dependencies:
  - name: common
    version: 0.1.0
    repository: file://../common
