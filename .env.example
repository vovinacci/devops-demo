# Optional overrides for the compose stack (copy to .env, which is
# gitignored). Defaults below match the values baked into the Makefile
# local test targets -- override only if you know why.

POSTGRES_DB=appdb
POSTGRES_USER=app
POSTGRES_PASSWORD=app
